runtime: "container"
build:
  dockerfile: "Dockerfile"           # Path to your Dockerfile
  dockerBuildPath: "."               # Docker build context
startCommand:
  type: "http"
  configSchema:
    type: object
    properties: {}

    commandFunction: |
      (config) => ({ command: 'uv', args: ['run', 'src/server.py'] 

    exampleConfig: {}